import { Outfit, Rock_Salt } from "next/font/google";

/**
 * Fuente para textos simples
 */
export const outfit = Outfit({
    subsets: ["latin"]
});

/**
 * Fuente del nombre de la app
 */
export const rock_salt = Rock_Salt({
    weight: "400",
    subsets: ["latin"],
});